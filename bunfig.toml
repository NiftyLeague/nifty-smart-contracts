[install]
# Hardhat resolves legacy bare OpenZeppelin aliases relative to dependencies.
# Keep this workspace's virtual store local so those aliases remain reachable.
globalStore = false
linker = "isolated"
