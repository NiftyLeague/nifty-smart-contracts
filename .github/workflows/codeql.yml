name: Code Foundry

on:
  push:
    branches: [main, staging]
  pull_request:
    branches: [staging]
  schedule:
    - cron: '31 6 * * 1'
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  packages: read
  security-events: write

jobs:
  codeql:
    name: CodeQL
    uses: 0xPlayerOne/code-foundry/.github/workflows/codeql.yml@v0.30.3
    with:
      runtime-repository: 0xPlayerOne/code-foundry
      runtime-ref: v0.30.3
      runner: ubuntu-latest
    secrets: inherit
