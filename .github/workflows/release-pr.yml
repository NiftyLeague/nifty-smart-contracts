name: Release PR

on:
  push:
    branches: [staging]

permissions:
  contents: read
  pull-requests: write

jobs:
  release-pr:
    name: Release PR
    uses: 0xPlayerOne/code-foundry/.github/workflows/release-pr.yml@v0.27.9
    with:
      runner: ubuntu-slim
    secrets: inherit
