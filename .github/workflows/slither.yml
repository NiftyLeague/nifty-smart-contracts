name: Slither Analysis

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: slither-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      security-events: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.3.14'

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build the contracts
        run: bunx hardhat compile

      - name: Prepare Slither artifacts
        run: node scripts/prepare-slither-build-info.mjs

      - name: Setup uv
        uses: astral-sh/setup-uv@ae62891fec2bb8e7d6c99fc78c9fec3a63790f8d # v10.0.0
        with:
          version: 0.11.32
          enable-cache: true
          cache-dependency-glob: .github/workflows/slither.yml

      - name: Run Slither
        id: slither
        shell: bash
        run: |
          uv tool run --from slither-analyzer==0.11.5 \
            slither . \
            --ignore-compile \
            --hardhat-artifacts-directory artifacts/slither \
            --sarif=results.sarif \
            --fail-high
          echo 'sarif=results.sarif' >> "$GITHUB_OUTPUT"

      - name: Upload SARIF file
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: ${{ steps.slither.outputs.sarif }}
          category: /language:slither
