name: CI

on:
  push:
    branches: [main, staging]
  pull_request:
    branches: [main, staging]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    if: github.event_name != 'pull_request' || github.head_ref != 'staging'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
<<<<<<< HEAD
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.14"
      - name: Install dependencies
        run: bun install
      - name: Hardhat compile
        run: bun build
      - name: Run Prettier
        run: bun format
      - name: Lint TS Scripts
        run: bun lint:ts
      - name: Lint Solidity files
        run: bun lint:sol
      - name: Check TypeScript
        run: bun type-check
      - name: Run Hardhat tests
        run: bun test
        env:
          TENDERLY_ACCESS_KEY: ${{ secrets.TENDERLY_ACCESS_KEY }}
      - name: Check Solidity coverage
        run: bun coverage
      - name: Test contract deployment
        continue-on-error: false
        run: bun hardhat run --network hardhat src/scripts/deploy_test.ts
=======
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Install pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 11.13.1
          run_install: false
      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: pnpm
      - name: Enable Yarn for Immutable's vendored Seaport build
        run: corepack enable yarn && corepack install --global yarn@1.22.22
      - name: Install dependencies
        run: pnpm install --frozen-lockfile --strict-peer-dependencies
      - name: Check formatting
        run: pnpm format:check
      - name: Lint TypeScript
        run: pnpm lint:ts:check
      - name: Lint Solidity
        run: pnpm lint:sol
      - name: Compile contracts
        run: pnpm build
      - name: Type-check
        run: pnpm type-check
      - name: Test with coverage
        run: pnpm test:coverage
      - name: Test contract deployment
        run: pnpm hardhat run --no-compile --network hardhat src/scripts/deploy_test.ts
      - name: Audit production dependencies
        run: pnpm audit --prod --audit-level=high
>>>>>>> origin/staging
